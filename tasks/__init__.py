"""Standalone, importable pipeline tasks for the lead generation system.

Each module exposes pure functions with explicit inputs/outputs so they can be
used directly (see ``run_local.py``) or wired into an Airflow DAG.
"""
